# Epic 8 回顾报告 — 参数提取节点自定义提示词增强

**日期：** 2026-03-12
**主持人：** Bob (Scrum Master)
**参与者：** Alice (PO), Charlie (Senior Dev), Amelia (Dev), Dana (QA), Elena (Junior Dev), Boss (Project Lead)

---

## Epic 摘要

**Epic 8: 参数提取节点自定义提示词增强 (Parameter Extraction Custom Prompt)**

编排工程师在配置"参数提取"节点时，能够自定义用于参数提取的提示词模板，替代系统默认的固定提示词，从而根据具体业务场景精准控制参数提取的逻辑与输出质量。

**覆盖需求：** FR22

### 交付指标

| 指标 | 数值 |
|------|------|
| 完成 Story | 2/2 (100%) |
| 改动文件数 | 5 |
| Code Review 发现 | 1 HIGH + 2 MEDIUM + 2 LOW（全部已修复）|
| 阻塞问题 | 0 |
| 生产事件 | 0 |
| 新增技术债务 | 0 |
| 调试问题 | 0（两个 Story 均一次实现成功）|

### Story 清单

| Story | 名称 | 范围 | 状态 |
|-------|------|------|------|
| 8.1 | 参数提取节点前端 - 新增自定义提示词配置界面 | 前端 (Vue + i18n) | done |
| 8.2 | 参数提取节点后端 - 运行时使用自定义提示词执行提取 | 后端 (Python) | done |

### 改动文件

- `ui/src/workflow/nodes/parameter-extraction-node/index.vue`
- `ui/src/locales/lang/zh-CN/workflow.ts`
- `ui/src/locales/lang/zh-Hant/workflow.ts`
- `ui/src/locales/lang/en-US/workflow.ts`
- `apps/application/flow/step_node/parameter_extraction_node/impl/base_parameter_extraction_node.py`

---

## 做得好的（What Went Well）

1. **模式复用策略再次成功** — Story 8.1 参照 Epic 7 的 `TextInputConstructor.vue` radio group 模式，Story 8.2 参照 Story 7.2 的 `self.node_params` 直接访问模式，两个 Story 均一次实现成功，零调试
2. **前后端解耦清晰** — 与 Epic 7 完全一致的前后端分拆策略，Story 8.1 只改前端文件，Story 8.2 只改一个 Python 文件，两个 Story 完全独立
3. **Code Review 持续有效** — 捕获 5 项改进全部修复，尤其是 Story 8.2 的 Jinja2 模板语法错误安全回退（M1）是重要的健壮性提升
4. **防御性编程到位** — Story 8.2 覆盖了空字符串、None、纯空白字符串、Jinja2 语法错误等多种回退场景，比基本需求更健壮
5. **架构理解精准** — Story 8.2 发现无需修改序列化器，直接通过 `self.node_params` 访问完整 `node_data`，避免了不必要的改动

---

## Code Review 详细发现

### Story 8.1 前端

| 级别 | 发现 | 处理 |
|------|------|------|
| H1 | linter 自动格式化导致提示词配置区域位置错误（移至 model_id 之后），应位于 ParametersFieldTable 之后 | ✅ 已修复 |
| M1 | disabled textarea 上冗余的 `:readonly="true"`（`disabled` 已足够） | ✅ 已修复 |

### Story 8.2 后端

| 级别 | 发现 | 处理 |
|------|------|------|
| M1 | 自定义模板 Jinja2 语法错误时缺少 try/except 安全回退到系统默认提示词 | ✅ 已修复 |
| L1 | `.strip()` 检查过滤纯空白字符串的 custom_prompt | ✅ 已修复 |
| L2 | `get_details()` 增加 `prompt_type` 诊断字段便于运行时排查 | ✅ 已修复 |

---

## 关键洞察

1. **模式复用是最高效的开发策略** — 连续两个 Epic 验证了当有良好参考实现时，新增功能几乎可以零摩擦交付。团队应持续维护"参考实现索引"
2. **前后端分拆策略成熟稳定** — Epic 7 和 Epic 8 采用相同的分拆方式（前端 UI 配置 + 后端运行时执行），已形成可复用的工作模式
3. **Code Review 是当前最有效的质量关卡** — 每个 Story 都捕获了有价值的改进，特别是安全性和健壮性方面

---

## 就绪度评估

| 维度 | 状态 | 说明 |
|------|------|------|
| 代码实现 | ✅ 完成 | 5 个文件改动已完成，Code Review 已通过 |
| 自动化测试 | ⚠️ 缺失 | 项目缺少前端测试框架，无自动化测试覆盖 |
| 手动验证 | ⏳ 待完成 | 10 项手动验证子任务待执行 |
| 技术健康度 | ✅ 良好 | 改动精简，无架构破坏，防御性编程到位 |
| 未解决阻塞 | ✅ 无 | 无阻塞问题 |

---

## 行动项

| # | 行动项 | 负责人 | 优先级 | 成功标准 |
|---|--------|--------|--------|----------|
| 1 | 更新 sprint-status 中 epic-8 状态为 done | Bob (SM) | 高 | sprint-status.yaml 中 epic-8 = done |
| 2 | 更新 epic-8-retrospective 状态为 done | Bob (SM) | 高 | sprint-status.yaml 中 epic-8-retrospective = done |
| 3 | 完成 Epic 8 手动验证测试（10 项） | Boss | 中 | 10 项验证子任务全部通过 |

### 团队约定

- 每个 Story 完成后，至少执行一轮手动冒烟测试再标记为 done
- Code Review 发现的模式性问题，应记录到技术债务清单并在后续迭代中排查修复
- Epic 全部 Story 完成时及时更新 sprint-status 状态

---

## 下一个 Epic 预览

当前路线图上 Epic 8 是最后一个已定义的 Epic。下一步工作方向待定。

---

## 下一步

1. **执行手动验证** — Boss 在浏览器中完成 10 项验证子任务
2. **更新 sprint-status** — 标记 Epic 8 和回顾为 done
3. **规划下一步工作** — Epic 9 尚未定义，待产品团队决定下一个方向

---

*本回顾文档由 Bob (Scrum Master) 于 2026-03-12 生成保存。*
