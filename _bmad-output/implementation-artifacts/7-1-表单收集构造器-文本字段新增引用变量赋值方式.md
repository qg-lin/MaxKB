# Story 7.1: 表单收集构造器 - 文本字段新增"引用变量"赋值方式

Status: done

## Story

As a 编排工程师,
I want 在配置"表单收集"节点的 TextInput 或 TextareaInput 字段时，能够切换赋值方式为"引用变量"并通过变量选择器选取上游节点的输出字段,
so that 表单字段的默认值可以在工作流执行时从上游节点动态注入，而不必每次手动维护静态文本.

## Acceptance Criteria

1. **Given** 我在"表单收集"节点的字段设置弹窗中，选择了 TextInput 或 TextareaInput 类型
   **When** 我查看"默认值"配置区域
   **Then** 应显示"赋值方式"单选组，包含"自定义"和"引用变量"两个选项（"引用变量"选项仅在工作流上下文中、即 `inject('getModel')` 有值时展示）

2. **And** 选择"引用变量"后，原文本输入框隐藏，显示 NodeCascader 变量选择器供选取上游节点字段

3. **And** 选择"自定义"时，恢复原有静态文本输入框，隐藏 NodeCascader

4. **And** 保存后，字段数据包含 `default_value_assignment_method: 'ref_variables'` 及 `default_value: [nodeId, ...fieldPath]` 数组结构，与 JsonInput 引用变量格式保持一致

5. **And** `rander()` 反序列化方法能正确还原 `default_value_assignment_method` 及对应的 `default_value`，编辑已有字段时赋值方式不丢失

## Tasks / Subtasks

- [x] Task 1: 修改 TextInputConstructor.vue 添加引用变量支持 (AC: #1, #2, #3, #4, #5)
  - [x] 1.1 引入 `inject` 和 `NodeCascader`，注入 `getModel`
  - [x] 1.2 添加 `assignment_method_option_list` computed，当 `getModel` 为 null 时只显示"自定义"
  - [x] 1.3 在默认值区域之前插入赋值方式 `el-radio-group`（仅 `getModel` 存在时显示）
  - [x] 1.4 用 `v-if="formValue.default_value_assignment_method == 'ref_variables'"` 控制 NodeCascader 显示
  - [x] 1.5 用 `v-if="formValue.default_value_assignment_method == 'custom'"` 保护原有 `el-input` 区块（含 `show_default_value` checkbox）
  - [x] 1.6 `getData()` 中增加 `default_value_assignment_method` 字段输出
  - [x] 1.7 `rander()` 中还原 `default_value_assignment_method`（缺省为 `'custom'`）
  - [x] 1.8 `onMounted` 中初始化 `default_value_assignment_method = 'custom'`

- [x] Task 2: 修改 TextareaInputConstructor.vue 添加引用变量支持 (AC: #1, #2, #3, #4, #5)
  - [x] 2.1 与 TextInputConstructor 完全相同的改动逻辑，区分点仅为 `input_type: 'TextareaInput'` 及文本区 `type="textarea"`

- [ ] Task 3: 手动验证测试 (AC: 全部)
  - [ ] 3.1 新增 TextInput 字段 → 切换引用变量 → 选取上游节点字段 → 保存 → 检查 JSON 结构
  - [ ] 3.2 编辑已有引用变量字段 → 确认 `rander()` 正确还原显示状态
  - [ ] 3.3 非工作流上下文（`getModel` 为 null）→ 确认引用变量选项不出现
  - [ ] 3.4 TextareaInput 同上执行一遍

## Dev Notes

### 精准实现参考路径

**唯一参考实现（必须遵循此模式，不得自创）：**
- [Source: `ui/src/components/dynamics-form/constructor/items/JsonInputConstructor.vue`]

`JsonInputConstructor.vue` 是与本故事功能完全对齐的已有实现，它使用了：
- `default_value_assignment_method`（而非 SingleSelect 的 `assignment_method`）
- `inject('getModel')` 判断是否在工作流上下文中
- `NodeCascader` 组件作为变量选择器
- `getData()` 输出 `default_value_assignment_method`
- `rander()` 还原 `default_value_assignment_method || 'custom'`

**切勿参考** `SingleSelectConstructor.vue` 的 `assignment_method` 字段名（字段名不同，语义不同）。

### 需要改动的文件（仅限以下2个）

1. [TextInputConstructor.vue](ui/src/components/dynamics-form/constructor/items/TextInputConstructor.vue)
2. [TextareaInputConstructor.vue](ui/src/components/dynamics-form/constructor/items/TextareaInputConstructor.vue)

**明确不需要修改的文件（Story 7.2 的范围）：**
- `apps/application/flow/step_node/form_node/impl/base_form_node.py` 的 `reset_field()` 方法 → 这是 Story 7.2 的工作

### 关键数据结构对比

**当前 TextInputConstructor `getData()` 输出（改动前）：**
```json
{
  "input_type": "TextInput",
  "attrs": { "maxlength": 200, "minlength": 0, "show-word-limit": true },
  "default_value": "",
  "show_default_value": true,
  "props_info": { "rules": [...] }
}
```

**改动后，引用变量模式的 `getData()` 输出：**
```json
{
  "input_type": "TextInput",
  "attrs": { "maxlength": 200, "minlength": 0, "show-word-limit": true },
  "default_value": ["nodeId_abc", "output", "fieldName"],
  "show_default_value": true,
  "default_value_assignment_method": "ref_variables",
  "props_info": { "rules": [...] }
}
```

**改动后，自定义模式的 `getData()` 输出：**
```json
{
  "input_type": "TextInput",
  "attrs": { "maxlength": 200, "minlength": 0, "show-word-limit": true },
  "default_value": "静态文本",
  "show_default_value": true,
  "default_value_assignment_method": "custom",
  "props_info": { "rules": [...] }
}
```

### 关键组件注入链路

```
form-node/index.vue
  └── provide('getModel', () => props.nodeModel)   ← 已有，无需修改
        └── AddFormCollect.vue / EditFormCollect.vue
              └── DynamicsFormConstructor/index.vue
                    └── TextInputConstructor.vue    ← 通过 inject('getModel') 感知上下文
```

`inject('getModel')` 的返回值在非工作流场景（普通表单配置页）为 `undefined`/`null`，此时 `assignment_method_option_list` 仅包含"自定义"，不显示赋值方式切换控件。这与 `JsonInputConstructor.vue` 和 `SingleSelectConstructor.vue` 的一致行为对齐。

### NodeCascader 使用规范

```vue
<NodeCascader
  ref="nodeCascaderRef"
  :nodeModel="model"
  class="w-full"
  :placeholder="$t('workflow.variable.placeholder')"
  v-model="formValue.default_value"
/>
```

- 组件路径：`@/workflow/common/NodeCascader.vue`（已存在，直接引入）
- `model` computed：`getModel ? getModel() : null`
- `v-model` 绑定 `formValue.default_value`（数组格式：`[nodeId, ...path]`）

### 验证规则（引用变量模式）

与 `JsonInputConstructor.vue` 完全相同的规则：
```typescript
const default_ref_variables_value_rule = {
  required: true,
  validator: (rule: any, value: any, callback: any) => {
    if (!(Array.isArray(value) && value.length > 1)) {
      callback(t('workflow.variable.Referencing') + t('common.required'))
    }
    return true
  },
  trigger: 'blur',
}
```

### 国际化 i18n

无需添加新的翻译键，所有需要的 i18n key 已存在：
- `dynamicsForm.AssignmentMethod.label` → "赋值方式"
- `common.custom` → "自定义"
- `workflow.variable.Referencing` → "引用变量"
- `workflow.variable.placeholder` → NodeCascader 占位符
- `common.required` → "必填"

### 项目结构规范

- Vue3 组合式 API（`<script setup lang="ts">`）
- 前端命名：小驼峰 composables，大驼峰 Components
- 使用 `computed` 双向绑定 `formValue`（通过 `emit('update:modelValue')`）
- 不引入任何新依赖，仅使用项目已有的 Element Plus、NodeCascader、`@/locales`

### 与 Story 7.2 的边界说明

| 故事 | 范围 | 关键改动 |
|------|------|----------|
| Story 7.1（本故事） | 前端构造器 UI | TextInputConstructor、TextareaInputConstructor |
| Story 7.2（下个故事） | 后端运行时解析 | `base_form_node.py::reset_field()` |

两个故事相互独立，可分别实现和验证。

### References

- 参考实现：[JsonInputConstructor.vue](ui/src/components/dynamics-form/constructor/items/JsonInputConstructor.vue) — 完整的 `default_value_assignment_method` 模式
- 相似实现（字段名不同）：[SingleSelectConstructor.vue](ui/src/components/dynamics-form/constructor/items/SingleSelectConstructor.vue) — `assignment_method` 模式
- 目标文件1：[TextInputConstructor.vue](ui/src/components/dynamics-form/constructor/items/TextInputConstructor.vue)
- 目标文件2：[TextareaInputConstructor.vue](ui/src/components/dynamics-form/constructor/items/TextareaInputConstructor.vue)
- 注入来源：[form-node/index.vue:155](ui/src/workflow/nodes/form-node/index.vue#L155) — `provide('getModel', () => props.nodeModel)`
- NodeCascader：[NodeCascader.vue](ui/src/workflow/common/NodeCascader.vue)
- 后端节点（Story 7.2 范围）：[base_form_node.py](apps/application/flow/step_node/form_node/impl/base_form_node.py)
- Epics 定义：[Source: _bmad-output/planning-artifacts/epics.md#Story 7.1]

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6[1m]

### Debug Log References

无调试问题，实现顺利。参考 JsonInputConstructor.vue 的现有模式，完整移植至 TextInputConstructor 和 TextareaInputConstructor。

### Completion Notes List

- ✅ Task 1: TextInputConstructor.vue — 新增 `inject('getModel')`、`NodeCascader` 导入、`assignment_method_option_list` computed、`model` computed、`default_ref_variables_value_rule` 验证规则；模板中添加赋值方式 radio group（仅工作流上下文显示）、NodeCascader form item（`ref_variables` 模式）；原 el-input 区块加 `v-if="custom"` 保护；`getData()` 增加 `default_value_assignment_method` 输出；`rander()` 还原 `default_value_assignment_method || 'custom'`；`onMounted` 初始化为 `'custom'`
- ✅ Task 2: TextareaInputConstructor.vue — 与 Task 1 完全相同的改动逻辑，区别仅为 `input_type: 'TextareaInput'`、`type="textarea"` 及 `rows: 3`
- ⚠️ Task 3（手动验证）: 需 Boss 在浏览器中手动验证，详见 Task 3 子任务清单
- ✅ Code Review 修复: [M1] 添加 watch 监听赋值方式切换时重置 default_value（custom→'' / ref_variables→[]），防止类型残留导致 UX 异常；[M2] v-for 添加 :key="item.value" 绑定

### File List

- ui/src/components/dynamics-form/constructor/items/TextInputConstructor.vue
- ui/src/components/dynamics-form/constructor/items/TextareaInputConstructor.vue

## Change Log

- 2026-03-05: Story 7.1 实现完成 — TextInputConstructor 和 TextareaInputConstructor 新增"引用变量"赋值方式支持，与 JsonInputConstructor.vue 模式一致
- 2026-03-05: Code Review 完成 — 修复 2 个 MEDIUM 问题：[M1] 切换赋值方式时重置 default_value 防止类型残留；[M2] v-for 添加 :key 绑定。2 个 LOW 预存问题保留（rangeRules 死代码、nodeCascaderRef 未读取）

## Senior Developer Review (AI)

**Review Date:** 2026-03-05
**Reviewer:** claude-sonnet-4-6[1m]
**Outcome:** Approve (with fixes applied)

### AC Verification
- AC #1: ✅ IMPLEMENTED — Radio group + getModel 条件显示
- AC #2: ✅ IMPLEMENTED — ref_variables 显示 NodeCascader
- AC #3: ✅ IMPLEMENTED — custom 显示 el-input
- AC #4: ✅ IMPLEMENTED — getData() 输出正确数据结构
- AC #5: ✅ IMPLEMENTED — rander() 正确还原

### Action Items
- [x] [M1] 切换赋值方式时重置 default_value（已修复）
- [x] [M2] v-for 添加 :key 绑定（已修复）
- [ ] [L1] rangeRules 死代码（预存，不修复）
- [ ] [L2] nodeCascaderRef 未读取（预存，不修复）
